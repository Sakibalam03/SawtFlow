# Reference audio

Place one consented 8-12 second, single-speaker WAV at `reference.wav`.

Use a quiet recording with no music, clipping, or strong reverberation. Update
`configs/benchmark.yaml` with the exact words spoken before running IndicF5.

